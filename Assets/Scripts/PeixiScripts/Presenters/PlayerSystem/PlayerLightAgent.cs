using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace Peixi.Player
{
    /// <summary>
    ///   <para>根据时间调整光照</para>
    ///   <para>-当时间</para>
    /// </summary>
    public class PlayerLightAgent
    {
        // Start is called before the first frame update
        void Start()
        {

        }

        // Update is called once per frame
        void Update()
        {


        }
    }
}
